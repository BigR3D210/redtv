*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
app/build/
.externalNativeBuild
.cxx/
# Signing secrets - never commit the release keystore
keystore.properties
*.jks
# The debug keystore IS committed on purpose (stable signature for updates)
